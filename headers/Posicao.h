#include <vector>
#include <stdio.h>
#include <iostream>
#include <stdlib.h>
#include <vector>
using namespace std;
class Posicao
{
private:
    /* data */
public:
    int x,y,z;
    Posicao(int x, int y, int z);
    ~Posicao();
};